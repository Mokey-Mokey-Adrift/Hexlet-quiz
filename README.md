# Интерактивная викторина

## Описание проекта
Веб-приложение на чистом JavaScript для проведения викторины.
Вопросы загружаются из внешнего JSON-файла. Поддерживается поочерёдный вывод вопросов, подсчёт очков, подсветка правильных/неправильных ответов. Есть форма для добавления новых вопросов.

## Требования
- Современный браузер (Chrome, Firefox, Safari)
- Локальный запуск (без сервера)

## Установка и запуск

### Способ 1: Клонирование через git alias (рекомендуется)
Сначала настройте алиас (выполните один раз):

```bash
git config --global alias.quiz '!f() { git clone https://github.com/Mokey-Mokey-Adrift/Hexlet-quiz.git "$@"; }; f'

Если вы настроили алиас:
```bash
git quiz [имя_папки]
Например:
git quiz my-quiz
```
### Способ 2: Обычное клонирование
```bash
git clone https://github.com/Mokey-Mokey-Adrift/Hexlet-quiz.git
```
### Способ 3: Скачать ZIP
Нажмите зелёную кнопку Code → Download ZIP на странице репозитория и распакуйте архив.

## Запуск

Откройте файл index.html в браузере.
Нажмите «Загрузить вопросы» и выберите подготовленный JSON-файл с вопросами.
Отвечайте на вопросы, получайте очки.
При желании добавьте новый вопрос через форму.

Формат JSON-файла
```json
[
  {
    "question": "Текст вопроса",
    "options": ["Вариант 1", "Вариант 2", "Вариант 3", "Вариант 4"],
    "correctAnswer": 0
  }
]
```

## Технологии
- HTML5
- CSS3
- JavaScript (ES6+)
- LocalStorage
- FileReader API